<%= simple_form_for [club, expense] do |f| %>
  <%= f.input :amount, as: :integer %>
  <%= f.association :expense_reason, label_method: :reason, collection: @club.expense_reasons %>
  <%= f.button :submit, 'Salva spesa' %>
<% end %>
